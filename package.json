{
  "name": "nomadic-phone",
  "version": "1.0.0",
  "description": "A simple web-based phone system built with Node.js, React, and Twilio APIs",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "nodemon server/index.js",
    "client:dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "install:all": "npm install && cd client && npm install",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint server/ --ext .js && cd client && npm run build",
    "lint:fix": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\" \"client/src/**/*.{js,jsx,ts,tsx,css}\" && eslint server/ --ext .js --fix"
  },
  "keywords": [
    "twilio",
    "voice",
    "sms",
    "webrtc",
    "phone",
    "nomadic",
    "react",
    "nodejs"
  ],
  "author": "Asim Ali",
  "dependencies": {
    "axios": "^1.11.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "twilio": "^4.19.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "concurrently": "^8.2.2",
    "eslint": "^9.35.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.6.2",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
